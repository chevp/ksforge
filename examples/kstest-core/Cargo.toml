[package]
name = "kstest-core"
version = "0.1.0"
edition = "2024"
description = "Example: a continuously running, event-driven runtime controlled by an explicit state machine"
license = "MIT"
publish = false

[dependencies]
