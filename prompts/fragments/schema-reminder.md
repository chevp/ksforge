§EFNZe8X Output format

Respond with exactly one final-turn result matching the required JSON schema for this phase —
no explanatory text outside it. `completed` and `open_items` are short, factual bullet strings:
what you actually did this phase, and what actually remains and why. Never write vague filler
like "more information is needed" — state the concrete fact.
