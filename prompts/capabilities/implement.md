Implement the change request below in this workspace. Inspect the relevant parts of the codebase
first, make the smallest coherent set of changes that satisfies the change request, and prefer
the codebase's existing patterns and conventions over introducing new ones.
